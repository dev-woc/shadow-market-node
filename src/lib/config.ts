// GUIDED_MODE: when true, gates tabs/features behind narrative prerequisites
// (e.g. Marketplace requires isAdminUnlocked from Scenario 1's exploit, Seller
// Program requires hasWashedCredits from Scenario 3).
// Defaults to false — the site plays "open world" today; flip to true for a
// future guided/tutorial variant that enforces scenario order.
export const GUIDED_MODE = false;
